thorntail:
  http:
    port: 8888
  logging:
    pattern-formatters:
      default:
        pattern: '%d %-5p [%t] %c: %m%n'
    root-logger:
      level: INFO
      handlers:
        - CONSOLE
    console-handlers:
      CONSOLE:
        level: DEBUG
        named-formatter: default
    loggers:
      org.muuvy.backend:
        level: DEBUG
